'use strict';

const express = require("express")
const router = express.Router()
const {Thread} = require("../models/threadModel")
const Board = require("../models/boardModel")

router.route("/api/threads/:board"). post(async(res, rep) => {

})